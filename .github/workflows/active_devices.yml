name: Build UEFI for active devices
on:
  push:
    branches: ["main"]
    paths:
      - 'Binaries'
      - 'Common/**'
      - 'Mu_Basecore'
      - 'MuPatches/**'
      - 'Platforms/**'
      - 'Silicon/**'
      - 'Silicium-ACPI'
  workflow_dispatch:
  pull_request:
    types: [opened, reopened]
    paths:
      - 'Binaries'
      - 'Common/**'
      - 'Mu_Basecore'
      - 'MuPatches/**'
      - 'Platforms/**'
      - 'Silicon/**'
      - 'Silicium-ACPI'
env:
  CI_BUILD: true
jobs:
  build:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4

      

      - name: Setup environment
        run: bash ./setup_env.sh -p apt

      # 如果脚本支持 -d 参数指定设备，就加上；如果不支持，保持原样
      - name: Build UEFI for active devices
        run: bash ./build_active_devices.sh -r DEBUG

      - name: Upload UEFI images
        uses: actions/upload-artifact@v4
        with:
          name: uefi-images
          path: ./Mu-*
          compression-level: 9
